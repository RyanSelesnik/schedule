#!/bin/bash
# Study CLI wrapper
# Delegates to Python CLI in src/cli.py

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
BASE_DIR="$(dirname "$SCRIPT_DIR")"

# Run the Python CLI
exec python "$BASE_DIR/src/cli.py" "$@"
